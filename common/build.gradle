plugins {
    id 'java-library'
}

dependencies {
    // UI Lib dependency (Modrinth: https://modrinth.com/mod/ui-lib)
    implementation "maven.modrinth:${project.ui_lib_modrinth_slug}:${project.ui_lib_version}"
}
